{
  "name": "ROS2 SCARA Workspace",
  "dockerFile": "Dockerfile",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ros-humble-rviz2",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-iot.vscode-ros",
        "ms-vscode.cpptools",
        "ms-python.python"
      ]
    }
  }
}
